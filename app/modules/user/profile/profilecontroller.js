'use strict';

angular.module('newsyApp.controllers.profile', ['newsyApp.services.user'])
  .controller('ProfileController', ['$scope', 'loginService', '$location',
    function($scope, loginService, $location) {

      
    }])
  